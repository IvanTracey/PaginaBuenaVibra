- Cuando linkeo el CSS tiene que estar luego de la tipografia y del bootstrap.
- Existe un h1 por html
- Los H tienen que estar en orden, no puede existir un h5 si no existe h2, h3, h4.
- Al usar imágenes en la pagina, no deben exceder los 500kB, hay que comprimirla con 


SCSS
- Cuando se trabaja con scss, recordar hacer el watch para que se vean reflejados los cambios en la pagina. (sass --watch scss/archivo.scss css/archivo.css)
- En el nesting, cuando se reusa una clase para las pseudoclases, por ejemplo, no hay que repetir su nombre sino que se  usa &

GIT
init					// Para comenzar el repo local
checkout -c "rama_nueva"	
git status
add . 					
commit -m "mensaje descriptivo"
branch main				
//Debo crear un repo en github y ese link uso abajo
remote add origin https://github.com/IvanTracey//......		// Conectar con github
git push -u origin main						// la primera vez, desp solo push